/* ClipForge Auto Edit: análisis automático al importar y control visual de silencios. */
(function(){
'use strict';
const $=id=>document.getElementById(id);let silences=[];let selectedSilences=new Set();let analyzing=false;const AUTO={threshold:-35,minDuration:.65};
const fmt=s=>{s=Math.max(0,Number(s)||0);return `${String(Math.floor(s/60)).padStart(2,'0')}:${String(Math.floor(s%60)).padStart(2,'0')}`};
function notify(t,type=''){const x=$('statusBar');if(!x)return;x.textContent=t;x.className=`status-bar ${type}`;x.classList.remove('hidden');clearTimeout(notify.t);notify.t=setTimeout(()=>x.classList.add('hidden'),4200)}
function ensurePanel(){if($('autoEditPanel'))return;const timeline=$('timelinePanel');if(!timeline)return;const panel=document.createElement('div');panel.id='autoEditPanel';panel.className='auto-panel';panel.innerHTML=`<div class="auto-head"><div><div class="auto-title">⚡ Auto Edit</div><div class="auto-sub">Analiza el video automáticamente y convierte las pausas en decisiones de edición.</div></div><span class="auto-badge">Reel workflow</span></div><div class="silence-summary"><span class="summary-chip"><strong id="silenceCount">0</strong> silencios</span><span class="summary-chip"><strong id="silenceDuration">0.0 s</strong> detectados</span><span class="summary-chip"><strong id="silenceSelected">0</strong> seleccionados</span><span class="summary-chip"><strong id="segmentCount">0</strong> segmentos</span></div><div class="silence-controls"><button id="autoSelectAll" class="mini-btn">Eliminar todos</button><button id="autoSelectNone" class="mini-btn">Mantener todos</button><button id="autoApply" class="mini-btn primary">✂ Aplicar recorte</button><button id="autoReanalyze" class="mini-btn">↻ Analizar de nuevo</button><span id="autoStatus" class="status"></span></div><div class="auto-help">Rojo = silencio detectado. Rojo intenso = se eliminará. Haz clic en cualquier zona roja para cambiar la decisión antes de aplicar el recorte.</div>`;timeline.appendChild(panel);$('autoSelectAll').onclick=()=>{silences.forEach((_,i)=>selectedSilences.add(i));renderSilenceLayer();updateSummary()};$('autoSelectNone').onclick=()=>{selectedSilences.clear();renderSilenceLayer();updateSummary()};$('autoApply').onclick=applySelected;$('autoReanalyze').onclick=()=>analyze(true)}
function ensureLayer(){const tracks=$('tracks');if(!tracks||$('silenceLayer'))return;const layer=document.createElement('div');layer.id='silenceLayer';layer.className='timeline-silence-layer';tracks.appendChild(layer)}
function renderSilenceLayer(){ensureLayer();const layer=$('silenceLayer');if(!layer)return;layer.innerHTML='';const d=state.duration||0;if(!d)return;silences.forEach((s,i)=>{const e=document.createElement('button');e.type='button';e.className=`timeline-silence-mark ${selectedSilences.has(i)?'selected':''}`;e.style.left=(s.start/d*100)+'%';e.style.width=Math.max(.25,(s.end-s.start)/d*100)+'%';e.title=`${fmt(s.start)} → ${fmt(s.end)} · ${s.duration.toFixed(2)} s`;const tag=document.createElement('span');tag.className='silence-tag';tag.textContent=selectedSilences.has(i)?`ELIMINAR · ${s.duration.toFixed(1)}s`:`MANTENER · ${s.duration.toFixed(1)}s`;e.appendChild(tag);e.onclick=ev=>{ev.stopPropagation();if(selectedSilences.has(i))selectedSilences.delete(i);else selectedSilences.add(i);renderSilenceLayer();updateSummary();};layer.appendChild(e)})}
function updateSummary(){ensurePanel();const total=silences.reduce((n,s)=>n+Number(s.duration||0),0);if($('silenceCount'))$('silenceCount').textContent=silences.length;if($('silenceDuration'))$('silenceDuration').textContent=total.toFixed(1)+' s';if($('silenceSelected'))$('silenceSelected').textContent=selectedSilences.size;if($('segmentCount'))$('segmentCount').textContent=state.clips.filter(c=>!c.deleted&&c.end>c.start).length}
function splitAt(t){const c=state.clips.find(c=>!c.deleted&&c.start+0.0001<t&&t<c.end-0.0001);if(!c)return;const i=state.clips.indexOf(c);state.clips.splice(i,1,{...c,id:uid(),end:t},{...c,id:uid(),start:t})}
function buildSegmentsFromAnalysis(){[...silences].sort((a,b)=>a.start-b.start).forEach(s=>{splitAt(s.start);splitAt(s.end)});draw()}
async function analyze(showNotice=false){if(analyzing||!state.videoId||!state.duration)return;analyzing=true;ensurePanel();$('autoStatus').textContent='Analizando audio con FFmpeg…';try{const fd=new FormData();fd.append('id',state.videoId);fd.append('threshold',AUTO.threshold);fd.append('min_duration',AUTO.minDuration);const r=await fetch('api/analyze.php',{method:'POST',body:fd});let d={};try{d=await r.json()}catch{}if(!r.ok||!d.ok)throw Error(d.error||`Error HTTP ${r.status}`);silences=(d.silences||[]).map(s=>({start:+s.start,end:+s.end,duration:+s.duration||(+s.end-+s.start)}));selectedSilences=new Set(silences.map((_,i)=>i));buildSegmentsFromAnalysis();$('autoStatus').textContent=silences.length?'Análisis automático listo. Revisa los rojos antes de aplicar.':'No se detectaron silencios con estos parámetros.';updateSummary();renderSilenceLayer();if(showNotice)notify(`${silences.length} silencios detectados`,'success')}catch(e){$('autoStatus').textContent='Error: '+e.message;notify('No fue posible analizar el video: '+e.message,'error')}finally{analyzing=false}}
function applySelected(){if(!selectedSilences.size)return notify('No hay silencios seleccionados para eliminar.','error');pushHistory();const chosen=[...selectedSilences].map(i=>silences[i]).sort((a,b)=>a.start-b.start);chosen.forEach(s=>{splitAt(s.start);splitAt(s.end)});state.clips.forEach(c=>{if(c.deleted)return;const hit=chosen.some(s=>c.start>=s.start-0.0005&&c.end<=s.end+0.0005);if(hit)c.deleted=true});state.selected=null;draw();renderSilenceLayer();updateSummary();notify(`Se marcaron ${chosen.length} silencios para eliminar`,'success')}
function hook(){ensurePanel();ensureLayer();video.addEventListener('loadedmetadata',()=>setTimeout(()=>analyze(false),120));video.addEventListener('durationchange',()=>setTimeout(()=>{if(state.duration&&!silences.length)analyze(false)},120));window.addEventListener('resize',renderSilenceLayer);const target=$('timelinePanel')||document.body;new MutationObserver(()=>{ensurePanel();ensureLayer();updateSummary();renderSilenceLayer()}).observe(target,{childList:true,subtree:true});ensurePanel();updateSummary();renderSilenceLayer()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',hook);else hook();
})();
